name: Hello ZeroOps 
on : 
  pull_request:
  push:
    branches: [main]
env:
    API_KEY: ${{ secrets.ABDRAHMAN }}
jobs:
  backend:
    
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/checkout@v4
            

      - run: 'echo "key length: ${API_KEY}"'
 

name: Hello ZeroOps 
on : push
env:
    API_KEY: ${{ secrets.ABDRAHMAN }}
jobs:
  backend:
    
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/checkout@v4
            

      - run: 'echo "key abdrahman: ${API_KEY}"'
 

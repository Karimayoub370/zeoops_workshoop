name: Hello ZeroOps
on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: action/checkout
        uses: action/checkout@v4
      - run: |
            python3 --version
            pwd
            ls -la